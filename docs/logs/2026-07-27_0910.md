---
prev: "[[2026-07-27_0900]]"
---
# 2026-07-27: タイマーに「シャットダウン前に通知を出す」チェックを追加

## 決定事項とWhy

ユーザー依頼「タイマーの設定にシャットダウン前に通知を出すかどうかのチェックボックスを追加して」。ユーザー判断: **通知=15秒の猶予表示そのもの**(OFFなら猶予も表示もなく即シャットダウン。OS通知の追加ではない)。

- `timers.js`: `actions.notifyBeforeShutdown` を追加。未指定は既存値→なければ `true`(既存タイマーは今までどおり猶予つき)
- `browser.js` `fireTimer()`: OFFのときは `timerPanel.ring()` も `registerGrace()` も呼ばず、`run()` の直後に `settleAfterRing()` で決着させる(繰り返しアラームは次回時刻へ進む)
- `sidepanel.html` / `sidepanel.js`: 「シャットダウンする」の下に確認チェックと並べたサブ項目として追加(シャットダウンOFFのときは隠す)。既定はON
- `scripts/test-timer.js`: 5項目追加(設定の保存 / OFFで猶予を作らない / OFFで即決着 / 既定はON / ONで猶予を作る)。全31項目OK

## タスク進捗状況

完了。
