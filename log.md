
## 現在の状態(2026-07-21 時点)

Electron 43.1.0 / Windows。`npm start` で起動、`npm run start:verify` で全レンダラーのコンソールエラーをターミナルに出して起動(検証用)、`npm run start:debug` でCDP用に起動。**配布: `npm run dist` でNSISインストーラー(dist/Roopie Setup x.x.x.exe)をビルド。リリースは version を上げて `npm run release`(要GH_TOKEN)or `gh release create`(latest.yml必須)。インストール版はGitHub Releasesを起動時+4時間ごとに確認して自動アップデート(updater.js)**。
最新: **再読み込みボタンの右にホームボタン(スタート画面に戻る)を追加**(`#home-btn`、既存の`window.roopie.navigate('roopie://newtab')`をそのまま利用。アドレスバーに直接入力した場合と同じ経路なので内部ページ用preloadの扱いなども素通しで正しく動く)。検証: 新規`scripts/test-home-button.js`。直前は**拡張機能管理をリッチ化(Edge風。一時無効化・詳細/権限表示・オプションページ・ピン留め集約)**。①**一時無効化**: Electron自体には「無効化」の概念が無い(読み込み/読み込み解除のみ)ため、`disabledExtensions`設定(プロファイル別)+`session.extensions.removeExtension()`/`loadExtension()`で自作。無効化するとsession.extensionsから消えて名前もパスも引けなくなるため、`ExtensionSupport`に`session -> Map(拡張ID -> メタデータ)`のキャッシュを追加し、一覧表示(無効化中も出す)と再有効化(ディスク上のパスが要る)の両方に使う。起動時は`attach(session, profileId, disabledIds)`で一度読み込んでメタデータを控えてから対象を外す。②**詳細/権限表示**: カードに「詳細」展開で拡張ID・`permissions`+`host_permissions`の一覧を表示。③**オプションページ**: `options_page`(MV2)/`options_ui.page`(MV3)の両形式に対応したURLを組み立て、ボタンから新規タブで開く。④**ピン留め集約**: 従来パズルボタンのメニューにしかなかったツールバーピン留め切替を設定画面のカードにも追加(同じ`pinnedExtensions`設定・`extensions:set-pinned`を共有)。検証: 新規`scripts/test-extensions-rich.js`(ネットワーク不要、ローカルのダミー拡張で無効化↔再有効化・起動時フィルタ・メタデータ保持を確認)。既存`scripts/test-extensions-menu.js`にもリグレッションなし。直前は**タブバーのアイコン/タイトルが「表示できてる時とできてないとき」がある報告を調査・修正**。原因は今セッションで入れた休止(不活性化)機能で、裏で開いた/セッション復元でフォーカスしなかったタブは実際に選ぶまで読み込まれないため`favicon`が`null`(文字アイコン止まり)、タイトルもホスト名止まりになっていた。①**休止中タブのfavicon**: 同じオリジンの履歴(`history.js`)から直近のfaviconを推測して仮表示するよう`history.faviconForOrigin(url)`を追加(実際に読み込まれれば正しいものに置き換わる)。②**起動時のセッション復元は「読み込まなくても」実際のタイトル・faviconを出す**: `snapshotTabs()`がタイトル・faviconも記録するようにし(`title: wc.getTitle()`, `favicon: tab.favicon`)、`restoreTabs()`→`createTab()`の`initialTitle`/`initialFavicon`としてそのまま渡して休止中タブの仮表示に使う(履歴推測より優先。ページ読み込みはしない、というユーザー指定どおり)。検証: 新規`scripts/test-dormant-favicon.js`/`test-restore-title-favicon.js`。直前は**F5キーを再読み込みの既定ショートカットに追加**(設定で変更できる`reload`コマンドの既定はCtrl+Rのみだったため、Chrome/Edge同様のF5が効いていなかった。`zoomIn`のCmdOrCtrl+=と同じ手法=`menu.js`に非表示の複製メニュー項目を追加し、設定画面のカスタマイズ対象(`keybindings.js`)には含めず常時Ctrl+Rと並行して効かせる)。検証: 新規`scripts/test-f5-reload.js`。直前は**裏で開いた/復元・休止復帰したタブが、見ていない間に自動再生する問題を「一時停止」方式で作り直し**(ミュート方式は「勝手にミュートにしないで」とフィードバックがあり不採用に)。まず`autoplayPolicy: 'document-user-activation-required'`で対処したつもりだったが実際のYouTubeで効いておらず(**data: URLでは効くがhttp/https実サイトには効かない**。メインプロセスの`loadURL()`遷移はこのゲートを素通りする)、次に**ミュートで抑える方式**に変えたが「勝手にミュートにしないで」と却下され、**実際にvideo/audio要素を検出してpause()する方式**に落ち着いた。裏で開いた/復元でフォーカスしなかった/休止から復帰したタブは`tab.autoPauseMedia`フラグを持ち、`media-started-playing`(Chromiumの再生開始通知。ミュート済み無音要素でも飛ぶ既存の仕組みを流用)をきっかけに全フレーム(shadow DOM含む)を見て**再生中でまだミュートされていないvideo/audioだけをpause()**する(装飾目的の無音背景動画などは触らない)。ミュートは一切しない。**そのタブ自身への実際の操作(クリック・キー入力)があった時点でフラグが下り、以後は普通に自動再生できる**(`armAutoPauseRelease`/`disarmAutoPause`/`pauseAutoplayedMedia`。タブバー側の切り替えクリックはページへの操作ではないため対象外)。`restoreTabs()`(セッション復元)もフォーカスするタブ以外は`background:true`と同じ経路に統一。**調査中に見つけた副次的な事実**: 実際のYouTubeサイトは自動再生できるのに自前のテスト用ローカルオリジンはそもそも自動再生ポリシーに阻まれる、という非対称が発生($=$Chromiumのメディアエンゲージメント実績によるオリジン単位の例外が働いている可能性が高い)。テストは実際の再生成否ではなく`pauseAutoplayedMedia`が呼ばれる/呼ばれないを直接確認する形に調整。**道連れで見つけた重大バグ(このセッション中に発覚・修正)**: 先に入れた「裏タブは選ぶまで読み込まない」機能(`tab.hibernated`)が、`electron-chrome-extensions`の「タブ登録時に必ず`chrome.tabs.onActivated`を発火する」仕様により**同期的に**`switchTab()`を誤って呼ばれてしまい、`activeTabId`は事後補正していたが**休止状態(`hibernated`)自体は補正されておらず即座に読み込まれていた**(=不活性化が実質効いていなかった)。`onTabCreated`呼び出し中だけ対象タブへの`switchTab`を無視する`_suppressActivationFor`ガードを追加して解消。検証: `scripts/test-autoplay-policy.js`/`test-restore-autoplay.js`(pause方式ベースに全面書き直し)。**副産物として調べた「YouTubeのUIが崩れがち」報告は結論そのまま有効**: 内蔵広告ブロック(Ghostery)のコスメティックフィルタ注入がYouTubeのPolymer製UIと衝突していたのが原因で、`adblock.js`で`loadCosmeticFilters = false`(ネットワーク遮断は維持)。検証: `scripts/test-adblock.js`。直前は**「YouTubeのUIが崩れがち」の報告を調査・修正**。①**内蔵広告ブロック(Ghostery)のコスメティックフィルタ(ページへのCSS/スクリプトレット注入)が原因と特定**(adblock ON/OFFのA/Bで実機比較し、ON時のみYouTubeで`Identifier 'JSONPath' has already been declared`や`dom-repeat: Maximum call stack size exceeded`が発生してUIが壊れることを確認。`adblock.js`で`blocker.config.loadCosmeticFilters = false`に変更し注入だけ止め、ネットワークレベルの広告/トラッカー遮断(`loadNetworkFilters`)はそのまま維持)。②**ホイールクリック等で裏に開いたタブが、見ていない間に勝手に音声/動画を自動再生しないように**(Electronの既定`autoplayPolicy: 'no-user-gesture-required'`は無条件に自動再生を許すため、Chrome/Firefox既定と同じ`document-user-activation-required`に変更。タブを開いた側=親ページへの操作はカウントされず、そのタブ自身への操作があって初めて再生できる)。検証: 新規`scripts/test-adblock.js`(コスメティック無効/ネットワーク有効の確認+実YouTubeでエラーが出ないこと)、`scripts/test-autoplay-policy.js`(裏タブは再生ブロック→切替後の操作で解禁、前面タブも未操作なら同様にブロック)、`scripts/test-youtube-diagnose.js`(原因調査用、F11のリサイズ回数もあわせて記録)。**副産物**: 調査中、このセッションで大量に起動したElectronテストプロセスが`taskkill`未実行のまま溜まり、以降のテストでポート競合・ハングを起こしていたのを発見(都度`taskkill /F /IM electron.exe`で解消)。直前は**タブ関連4件をまとめて追加**。①**タブをウィンドウの外へドラッグし、別ウィンドウの既存タブバーへドロップすると、そのウィンドウへ移動する**(HTML5 D&Dの`draggingId`はレンダラーごとのローカル変数でウィンドウをまたぐと相手側から見えないため、専用MIME`application/x-roopie-tab`でウィンドウID/タブIDを運ぶ。移動先の`ipc.js`側`tabs:move-from-window`で元ウィンドウのタブを閉じてURLだけ引き継ぎ新規作成(WebContentsViewはウィンドウをまたいで再利用できないため)。元ウィンドウの既存`drag-end`(タブバー下へのドロップ=新規ウィンドウ化)が同じドラッグに対して二重発火しないよう、移動確定時に`ctx.tabConsumedBy`を立てて`drag-end`側の40ms遅延処理内でガード)。②**タブバーのミュートボタンの隣に再生/一時停止ボタンを追加**(`media:state`をこれまでサイドパネル/フローティングプレイヤー専用だった内部preloadだけでなく、チロームUI本体のpreloadにも配線)。③**F11(OS全画面)中はページを画面いっぱいに広げ、タブバー/ツールバーはマウスが上端に近づいた間だけ一時表示**(`screen.getCursorScreenPoint()`を全画面中だけ80ms間隔でポーリングし、上端6px以内で表示・ツールバー高さ+24pxより離れたら非表示のヒステリシス。ページ側の全画面(YouTube等)とは別フラグで扱い、分割ペインの一時解除などpage-fullscreen固有の挙動には影響しない)。④**バックグラウンドで開いたタブ(中クリック等)は、実際にそのタブへ切り替えるまで読み込まない**(以前からあるタイマーの「タブを休止する」機能と同じ`tab.hibernated`/`hibernatedUrl`復元経路を新規タブ作成時にも適用し、`switchTab`の既存復元ロジックをそのまま再利用。内部ページ(roopie://)は対象外=即読み込み。休止中のタブバー表示はホスト名を仮タイトルに)。検証: 新規`scripts/test-tab-cross-window.js`/`test-tab-play-button.js`/`test-fullscreen-chrome.js`+既存`scripts/test-background-tab.js`にホスト名仮タイトル・内部ページ除外のケースを追加。直前は**メディアプレイヤーで複数タブを独立して扱えるように刷新**(従来は`pickMedia`で「1番優先度の高いタブ」だけを選んで単一状態として配信していたが、`refreshMedia`でタブごとに独立したエントリの配列(`ctx.mediaList`)を作るよう変更。ミュートは`tabManager.toggleMute`を再利用(音量スライダーはElectronにタブ単位のAPIが無く、ページJS注入だとWeb Audio制御サイトで効かないため見送り=ミュートのみ採用)。**タブ単位の「フローティング表示」切り替えも追加**(`mediaDockedOverrides` Map。既定値は従来のmediaDocked設定のまま、タブごとにオーバーライドできる。セッション限りで永続化はしない)。サイドパネルの「再生中」セクションは複数の`.now-playing-card`を並べ、各カードにミュートボタンとフローティング表示トグルを追加。フローティングパネル(`mediaplayer.js`)はtimer-panel.js同様、行数ぶん高さが伸びる可変サイズのリストに刷新(再生中のタブがアクティブ/分割相手の間はその行だけ非表示)。**道連れで見つけたバグ**: フローティングパネルはtimerpanel.js同様、表示直後にpull取得が無く空のまま描画されることがあったため`media:list` invokeハンドラを追加。**副作用として発覚した既存不具合**: 先日のCtrl+D修正(`bookmarks.existsAnywhere`追加)以降、`bookmarks`を独自スタブで渡す既存の検証スクリプト4本(`test-background-tab.js`/`test-fullscreen.js`/`test-gesture-actions.js`/`test-media-detect.js`)が`existsAnywhere is not a function`で例外停止していたのを発見しスタブを修正。検証: 新規`scripts/test-media-multi.js`(2タブが独立して一覧・ミュート・フローティング表示を扱えることを実UIで確認)+既存`scripts/test-media-player.js`/`test-media-detect.js`を新API向けに更新。直前は**ブックマークバーの表示/非表示を右クリックメニューから切り替えられるように追加**(Ctrl+Shift+Bと同じ設定を操作するチェックボックス付きメニュー。既存の`toolbar-context-menu.js`のパターンを踏襲)。直前は**タイマーパネル(サイドパネル一覧+フローティング表示)をiOSの時計アプリ風に刷新**。サイドパネル一覧は大きい数字(残り時間/経過時間)+小さい説明(名前 or 元の長さ)+丸い再生/一時停止ボタン(緑=開始、オレンジ=一時停止、赤=アラーム停止)のみのシンプルな行に(`timer-item-time`/`timer-item-sub`/`timer-circle-btn`)。行が煩雑にならないよう「リセット」はテキストボタンから右クリックメニュー(`showTimerMenu`)へ移動。フローティング表示はiOSロック画面のライブアクティビティ風に、黒い丸ピル+丸ボタン+オレンジの大きい時刻文字へ変更(`timerpanel.js`/`.timerp-row`)。**道連れで見つけたバグも修正**: フローティングパネルは表示されるまでViewが作られない設計のため、表示直後にタイマーの状態変化が無いと(pushのみでpull無しだったため)何も描画されないままになっていた。表示時に`listTimers()`で明示的に初期状態を取得するよう追加。検証: 既存の`scripts/test-timer-ui.js`にフローティング側の描画確認とスクリーンショットを追加。直前は**Ctrl+D案内を、フォルダの中(スタート画面のショートカット等のサブディレクトリ含む)に既にあるページでは出さないよう修正**(`bookmarks.find()`はトップレベル直下しか見ないため、通常フォルダやstartフォルダのサブフォルダに保存済みのページが再訪問のたびに案内されていた。フォルダ階層を問わず探す`existsAnywhere()`を追加し、案内の抑制判定(`did-navigate`時と`sendState`時の両方)だけをこちらに差し替え。**星アイコンの点灯/トグルは従来通り`find()`(トップレベルのみ)のまま**据え置き=フォルダ内保存ページで星を押すと重複追加されてしまう副作用を避けた。検証: 既存の`scripts/test-bookmark-hint.js`にフォルダ内保存のケースを追加(スタブの`bookmarks`に`existsAnywhere`を実装)。直前は**タブに音声再生中のスピーカーアイコンを追加、クリックでタブ単位のミュート切り替え**(`wc.on('audio-state-changed')` でタブごとに`isAudible`を追跡し`tabs:state`へ乗せる。アイコンは常時表示(ホバー不要、Edge同様)・クリックで`tabs:toggle-mute` IPC→`tabManager.toggleMute()`(既存)。**重要なバグ2件を道連れで修正**: (1) `electron-chrome-extensions`は新規タブ登録時に必ず`chrome.tabs.onActivated`を発火する仕様(`observeTab`内で無条件)で、その結果バックグラウンドで開いたはずのタブが即アクティブ化されてしまっていた(ホイールクリック新規タブ機能が実質効いていなかった)。`createTab`で拡張機能登録前後の`activeTabId`を比較し、背景タブなら元に戻す形で相殺(`tab-manager.js`)。(2) `probeMedia`がタブを閉じた直後の`media-paused`で`tab.view.webContents`が消えていて未捕捉例外になっていたのを`wc`のnullガード追加で修正。検証: `npx electron scripts/test-tab-audio.js`(AudioContextで実際に音を鳴らし、background作成→isAudible伝播→アイコン表示→クリックでミュート→再クリックで解除まで実UIで確認)。直前は**既定のブラウザ化のリクエストを追加**(パスワード保存の確認バーと同型のトップバー通知。`app.isDefaultProtocolClient('http'/'https')` で既定かどうかを判定し、未既定なら起動時にどれか1つのウィンドウで1回だけ表示。「既定のブラウザにする」で `setAsDefaultProtocolClient` を登録したうえで、Windowsは UserChoice 保護のため直接切り替えられないので `ms-settings:defaultapps` を開いてユーザーに選んでもらう。**うざくないよう1・2回目の「閉じる」は毎起動で出すが、2回見送ると2週間は再度出さない**(3回目以降も見送るたび2週間隔)。出し分けは`decidePrompt()`として副作用なしの純関数に切り出し(`default-browser.js`。状態は`userData/default-browser-state.json`)。Windowsの設定から直接既定に変えていた場合は見送り履歴を持ち越さずリセットする。検証: `npx electron scripts/test-default-browser.js`(出し分けロジック+実UI: 自動表示→「閉じる」で見送り記録→同起動中の2枚目では出さない→「既定にする」はOS呼び出しをスタブに差し替えて副作用なく確認)。直前は**サイドパネルに「タイマー」パネルを追加**(カウントダウン/時刻指定(曜日繰り返し可)/ストップウォッチの3種、複数登録可。時間になったときのアクションを複数選択可=音を鳴らす/タブを休止する(簡易実装。URLを保存してabout:blankへ退避し、タブを選び直すと復元)/ウィンドウを閉じる/特定のページを開く/シャットダウンする。**「ウィンドウを閉じる」「シャットダウン」は猶予15秒+キャンセル可能にしてから実行**し、シャットダウンは設定保存時にも追加確認チェックを必須化。実行中でサイドパネルを閉じている間はメディアプレイヤーと同じ流儀でフローティング表示(`timer-panel.js`。複数タイマーをリスト表示できる点がメディアプレイヤーとの違い。既定コーナーはtop-right、MediaPlayerのbottom-rightとぶつからないよう分離)。**tickはブラウザ全体で1本**(`browser.js`の`initData()`内、1秒間隔。ウィンドウが開いているプロファイルの分だけ進めるため、ウィンドウの無いプロファイルのタイマーは鳴らない)。**アプリが閉じていた間に予定時刻を過ぎていたタイマーは発火させず状態だけ進める**(`Timers.catchUp()`)。シャットダウン実行は`ROOPIE_TIMER_SHUTDOWN_DRYRUN=1`で無効化できるガード付き(検証ハーネス用)。検証: `npx electron scripts/test-timer.js`(エンジンのCRUD/tick/catchUp/危険アクションのグレース・キャンセル)、`npx electron scripts/test-timer-ui.js`(実UI: モーダル種別切替→保存→開始→フローティング表示→発火)。直前は**Googleアカウントの自動検出が再び機能していなかったのを修正**(前回7/17の修正=`session.fetch`を使う、まで直しても、GoogleのListAccounts APIはページの文脈を持たない素のリクエスト=`Sec-Fetch-Site: none`・Origin無し・「no-cors」形状だと、Cookieが正しく乗っていてもHTTP 400で拒否することが実機の送信ヘッダーキャプチャで判明。ネットワーク越しの検出を諦め、Googleの各ページがすでに描画している「Google アカウント: 名前 (メール)」ボタンのaria-label/titleをそのタブの`webContents.executeJavaScript`でDOM越しに読む方式に変更(`google-accounts.js`の`detectFromWebContents`/`isGoogleDomain`。ドメイン判定は元々`tab-manager.js`にあったものをここに統合)。**既知のトレードオフ: そのページで表示中の1アカウントしか拾えない**(ListAccountsのような複数同時ログインの全件取得はできなくなった)。検証: `npx electron scripts/test-google-detect.js`(ロジック単体)+ `ROOPIE_LOG_CONSOLE=1`での実機確認(設定画面にアカウントが表示されるまで確認)。直前は**タブバーの空き領域(ドラッグ検索・並べ替えの末尾)へドロップしても何も起きない不具合を修正**(`#tab-bar` の空きスペースはウィンドウ移動用の `-webkit-app-region: drag` になっており、そこではドロップイベント自体が拾えなかった。ドラッグセッション中(`document`のdragoverでtext/plain型を検知した時点)だけ `.dnd-armed` クラスでno-dragに切り替えるようにした。タブバー自身のdragoverで切り替えると空き領域へ直接進入したケースに間に合わないため、page側から上がってくる途中(ツールバー等)で先に検知するのがポイント。検証: `npm run start:verify` + `ROOPIE_LOG_CONSOLE=1` でdragover/dropを実機ログ確認)。直前は**ウィンドウ切り替え時にページコンテンツへフォーカスが戻らずYouTube等のショートカットがすぐ使えない不具合を修正**(`browser.js` に `window.on('focus')` を追加してアクティブタブのwebContentsへ明示フォーカス。オーバーレイ表示中は除外。検証: `npx electron scripts/test-focus-on-switch.js`)。直前は**拡張機能のパズルボタン(Edge風のピン留めメニュー)を仕上げた**(7/17のWIP `9c3c8ce` に欠けていた `#ext-menu` のCSSを追加。設定画面の `.ext-icon`(36px)と同名クラスがぶつかるため id で打ち消している。検証: `npx electron scripts/test-extensions-menu.js`)。直前は今日の変更のレビュー反映 = **Googleアカウントの自動検出が原理的に一度も動いていなかったのを修正**(`net.fetch` は第2引数の session を無視するので `session.fetch` へ)、シークレットのタブ切り離しが通常ウィンドウになる/ジェスチャーの1画面戻る進むが無反応/全画面のままタブ切替で戻れない/裏タブが0x0 などの重大バグも修正。同日にマウスジェスチャーの割当機能を9→37個、起動時のタブ復元をON/OFF設定化、動画プレイヤーの全フレーム探索+全画面対応、背景のカスタマイズ、Ctrl+D案内の抑制。さらに前は**ローンチ前の仕上げ = 初回起動のイントロ(`roopie://welcome`)、アップデート後の変更点表示(`roopie://whatsnew`)、NSISアシスト形式のインストーラー(独自の左側画像・ヘッダー画像)、設定画面の「Roopieについて」(バージョン/更新を確認/変更履歴/イントロ再表示)、README・LICENSE を追加**。直前はリリースを自動化。masterへpushするだけでGitHub Actionsがビルドして公開し、インストール済みのRoopieが自動更新する**(`.github/workflows/release.yml`)。バージョンは `0.1.<Actionsの実行番号>` をビルド時にだけ適用(package.jsonは書き換えずpushし返さないので無限ループしない)。※このワークフローはリネーム禁止(実行番号がリセットされてバージョンが逆行し、自動更新が止まる)。直前はサイドパネルに「トラッキング」セクションを追加=Cookieを既知トラッカー定義(約100社)で分類し、どの企業が自分に固有IDを付けているかを企業単位で表示・削除できる。あわせてスタート画面ニュースのRSSプリセットにANN/BBC/CNN/WSJ/NYタイムズを追加、ローカルサーバー検知の走査ポートを52種に拡充。直前はタブバーのドラッグD&Dを刷新=並べ替えでも「ドラッグ検索(選択テキストのドロップ)」でも、既存タブの間に差し込める共通UX。ドラッグ中はカーソル追従の挿入スロット(隙間)をリアルタイム表示し、並べ替え時はつまみ上げたタブを畳んで隙間化。検索ドロップは差し込み位置に新タブを開く(ipc `tabs:search-new-tab` にindex追加)。直前はNSISインストーラー化+GitHub Releases自動アップデート。直前はサイドパネルのブックマークをVSCodeエクスプローラー風ツリーに刷新=フォルダ階層・右クリックメニュー・インライン改名/フォルダ作成・D&D移動(564a6a5)。直前はブックマーク管理画面にスタート画面のショートカットを統合=1つのフォルダツリーで管理・相互移動(dd008ee)、グリッド目安幅をmin(700px, ウィンドウ幅-48px)に変更(2a67f8c)、サイドパネルのブックマークに追加モーダル(a48585c)。直前はショートカット・ウィジェットの追加を「+」アイコンから右クリックメニューに変更(9d0558e)。同日の一連の変更: グリッド座標モデル化+ドラッグ時のグリッド線・ドロップ先プレビュー+自動上詰め廃止(1589acb)→ウィジェットのドラッグリサイズ(6107ded)→アイコン最大サイズ1本化(f201587)→アイコンのネイティブドラッグ衝突を修正(85f5445)→追加を右クリックメニューに変更(9d0558e)。さらに前はスタートページのスワイプ切り替え+デフォルトアイコン拡大+共通アイコンピッカーのはみ出し修正(d8ebf71)、その前にグリッドの列数・行数を手動設定可能にした版(0fbad81、後にアイコン最大サイズ方式へ置き換え済み)。さらに前はプロファイル切り替えをEdge挙動に(切り替え=新しいウィンドウ、複数プロファイル同時利用。ウィンドウ単位のプロファイルにアーキテクチャ変更 324e356)。同日にパスワード/オートフィル拡充、スタート画面ウィジェットも実装依頼済みの未完了作業: なし

| 項目 | 状態 |
|---|---|
| Phase 1: 最小構成ブラウザ | ✅ 完了 |
| Chrome同等の基本機能 | ✅ 完了 |
| Phase 3: プロファイル機能 + 設定画面 | ✅ 完了 |
| Googleアカウントのプロファイル別管理 | ✅ 完了 |
| Phase 2: 拡張機能対応の検証 | ✅ 完了(結論: uBOは不可 → 内蔵広告ブロックで代替。コンテンツスクリプト型拡張は動く) |
| Phase 4: マウスジェスチャー(GUIカスタム対応) | ✅ 完了 |
| Phase 4: サイドパネル(ブックマーク/履歴/メモ/Webパネル) | ✅ 完了 |
| Phase 5: テーマ機能 + Bonjourr風スタートページ | ✅ 完了 |
| Phase 5: Tailwind CSS v4 + Preline UI 導入 | ✅ 完了 |
| Phase 5: UI改善(フレームレス化・SVGアイコン) | ✅ 完了 |
| Phase 6: パスワード保存 | ✅ 完了 |
| Phase 6: 残りのChrome機能(タブ並べ替え/複数ウィンドウ/シークレット) | ✅ 完了 |
| 拡張機能の管理UI(設定画面+ツールバーアイコン) | ✅ 完了 |
| UIをEdge風に刷新(ツールバー/ワークスペースピル/設定目次/サイドパネル) | ✅ 完了 |
| プロファイル/スタート画面/Googleアカウント連携の拡充 | ✅ 完了 |
| Tor接続のプロファイル別ON/OFF | ✅ 完了 |
| 右クリックメニューの充実 / ページQRコード生成 | ✅ 完了 |
| サイドパネルのリサイズ・ヘッダー / QRファイル名の自動設定 | ✅ 完了 |
| 配布(NSISインストーラー)+ GitHub Releases自動アップデート | ✅ 完了 |
| サイドパネルのトラッキング分析(Cookieのプロファイリング可視化) | ✅ 完了 |
| リリースの自動化(push → GitHub Actions → Releases → 自動更新) | ✅ 完了 |
| ローンチ準備(イントロ/変更点表示/インストーラーの体裁/README・LICENSE) | ✅ 完了 |

### 動く機能(一覧)

- **ブラウザ基本**: タブ(Ctrl+T/W/Tab、中クリックで閉じる、Ctrl+1〜9)、アドレスバー(URL判定/Google検索)、戻る・進む・再読み込み、ズーム(Ctrl+ +/−/0)、ページ内検索(Ctrl+F)、右クリックメニュー、印刷(Ctrl+P)、全画面、マウスの戻る/進むボタン
- **データ**: ブックマーク(Ctrl+D、バー Ctrl+Shift+B、管理 Ctrl+Shift+O)、履歴(Ctrl+H)、ダウンロード(Ctrl+J)。**管理画面は通常のブックマークとスタート画面のショートカットを1つのフォルダツリーで表示**(フォルダ折りたたみ・名前変更・削除・「移動」でブックマーク⇔スタート画面の各ページを行き来・検索は所在パス付きで横断。startルート自体は削除・改名不可)。サイドパネルのブックマークは「ブックマークを追加」ボタンからURL+名前のモーダルで追加可。検証: `npx electron scripts/test-bookmarks-manager.js`
- **プロファイル(Edge挙動)**: タブバー左端のワークスペース風ピルからプルダウンで切り替え。**切り替え=そのプロファイルの新しいウィンドウを開く**(既存ウィンドウは元のプロファイルのまま。複数プロファイルを同時に使える)。ウィンドウの無いプロファイルを開くと前回のタブ構成を復元(最後のウィンドウを閉じるときに保存)。プロファイル削除でそのウィンドウも閉じる。設定画面(Ctrl+,)で追加・名前変更・削除、共有トグル(ブックマーク/履歴/設定/パスワード/自動入力など。データ束はウィンドウのプロファイル単位)。設定画面のプロファイルカードから切り替えずにテーマカラー変更可。検証: `npx electron scripts/test-multi-profile.js`
- **タブバーの位置**: 上部(既定)/左側(縦)をツールバーのアイコンで即切り替え(グローバル設定)。縦タブ時は上部に専用ストリップを置かず、ツールバーがタイトルバー(ドラッグ領域)を兼ねる=デッドスペースなし。切替ボタン+ワークスペースピルはレール最上部の1行に入る
- **タブの切り離し**: タブをタブバーの外へドラッグ&ドロップすると新しいウィンドウとして開く(最後の1枚は不可)
- **Tor接続(プロファイル別)**: 設定画面のプロファイルカードの「Torで接続」トグルで、そのプロファイルの通信をTorネットワーク経由にできる。プロファイル(=セッション)ごとに独立(あるプロファイルはTor、別のプロファイルは直接接続を同時に使える)。使用中はワークスペースピルに🧅インジケーター。**Tor本体(tor.exe)を同梱しているので追加インストールは不要**(`scripts/fetch-tor.js` が公式Expert BundleからSHA256照合付きで取得 → `vendor/tor` → extraResourcesで同梱。**CIがリリースのたびに最新の安定版を取り直すのでRoopieの更新でTorも新しくなる**)。探索順は ①`%APPDATA%/Roopie/tor/tor.exe`(ユーザーの差し替え用) ②同梱 ③インストール済みTor Browser。なお起動時にまず9050/9150を見て、既に動いているTorがあればそれを使う。同梱バージョンは設定画面のTor状態表示に出る。ブリッジ(pluggable transports)とgeoipは同梱していない。検証: `npx electron scripts/test-tor.js`(実際にBootstrapped 100%まで確認。30〜90秒)
- **プロファイルアイコン**: 設定画面でアバターをクリックすると選択パネル(既定の絵文字24種+自由入力+画像アップロード+文字に戻す)。画像アップロード時はドラッグ&ズームのGUIクロップ(円形プレビュー)。ツールバー・プロファイルメニュー・設定画面すべてに反映
- **Googleアカウント**: ブラウザ全体に登録し、プロファイルごとに有効化+プライマリ選択。**ログイン中のアカウントを自動検出して登録・有効化**(設定画面を開いたとき、およびタブがgoogle.com系ドメインへ遷移したときの2箇所でチェック。手動登録も引き続き可能)
- **マウスジェスチャー**: 右クリック+ドラッグ(軌跡+アクション名を表示)。既定: ←戻る / →進む / ↓新しいタブ / ↓→タブを閉じる / ↑↓再読み込み。設定画面のGUIで自由に追加・変更・削除でき、共有トグルで全プロファイル共通にもできる
- **サイドパネル**(F4 / ツールバーの◨ボタン。Vivaldiのパネル準拠): アイコンレールの並びはブックマーク→ダウンロード→履歴→メモ→リーディングリスト→トラッキング→タイマー→ピン留めWebパネル→「+」(ウェブパネル追加)。自動保存メモ、Webパネル(任意サイトの常駐表示・複数登録)。同じアイコンをもう一度押すと折りたたみ(Vivaldi同様)。**タイマー**: カウントダウン/時刻指定(曜日繰り返し)/ストップウォッチを複数登録・同時実行可。行の開始/一時停止/リセット/削除、右クリックで複製・削除。発火時アクション(音/タブ休止/ウィンドウを閉じる/ページを開く/シャットダウン)を複数選択可、危険なもの(ウィンドウを閉じる・シャットダウン)は猶予15秒+キャンセル可。実行中でパネルを閉じている間はフローティングのミニウィンドウで表示(詳細は本ファイル冒頭「最新」を参照)。**ブックマークパネルはVSCodeエクスプローラー風ツリー**(ブックマーク全体+スタート画面のショートカットをフォルダ階層で表示。ツールバー=新規ブックマーク/新規フォルダ/すべて折りたたむ、右クリックメニュー、インラインで改名・フォルダ作成、D&Dで別フォルダへ移動。追加モーダルは追加先フォルダ対応)。**Webパネルの管理画面は無し**: 追加は「+」またはレール右クリック、編集(名前/アイコン/URL)・削除はピン留めアイコンの右クリックメニューから。アイコンは既定でリンク先のfavicon、プロファイルと同じ共通ピッカー(絵文字24種+自由入力+画像アップロード→ドラッグ&ズームのGUIクロップ。ボタン行はflex-wrapでパネル幅からはみ出さない)で変更可。データはプロファイル単位。※AIアシスタント機能は2026-07-17に廃止
- **広告ブロック(内蔵)**: EasyList等のフィルタで広告・トラッカーを遮断(@ghostery/adblocker-electron)。設定画面のトグルでON/OFF(既定ON、プロファイル単位)
- **トラッキング分析(サイドパネル「トラッキング」)**: セッションのCookieを既知トラッカー定義(約100社。`src/main/trackers.js`)に照らして企業単位にまとめ、「何社が自分に固有IDを付けているか」「サイトをまたいで使えるCookieは何件か」「いつまで識別されるか」を表示。企業ごとに業種・Cookie一覧・識別子バッジを展開でき、企業単位/一括でCookieを削除できる。履歴からローカル辞書だけで興味カテゴリも推定(AI・外部送信は使わない)。**判定は「Cookieのドメイン≠訪問サイト」ではなく既知トラッカー定義で行う**(google.comのNIDやnhk.or.jpのAMCV_のように最大手ほど一次Cookieとして入るため、ドメイン差分では取りこぼす)。サイトをまたげるかは SameSite=None で判定。検証: `npx electron scripts/test-trackers-panel.js`、トラッカー定義の実データ調整: `npx electron scripts/inspect-cookies.js [--probe]`
- **Chrome拡張機能(部分対応)**: 設定画面の「拡張機能」セクションでウェブストアIDを指定してインストール・一覧・削除。ツールバーに拡張アイコン(`<browser-action-list>`。クリックでポップアップ)。**Edge風のピン留め**: ツールバーのパズルボタンで拡張の一覧が開き、行クリックで実行、ピンでツールバーに出す/出さないを切り替え(ピン留めしたものだけツールバーに並ぶ。設定はプロファイル単位)。拡張が0件のときとシークレットではパズルボタン自体を出さない。コンテンツスクリプト型拡張(Dark Reader等)が動く。検証: `npx electron scripts/test-extensions-menu.js`。uBlock Origin等のブロッキング型は不可(下記Phase 2検証を参照)
- **パスワードマネージャー**: ログイン送信を検出して保存確認バー(保存/今はしない/このサイトでは保存しない)。保存1件なら読み込み時に自動入力、複数はログイン欄フォーカスのドロップダウンから選択(最近使った順)。新規登録フォームで強力なパスワード生成。safeStorage暗号化。設定画面で検索・表示・編集・削除・CSVエクスポート/インポート・除外サイト解除
- **オートフィル(住所・カード)**: 設定画面「自動入力」で住所・個人情報とクレジットカードを登録(カード番号は暗号化、CVCは保存しない)。フォーム欄フォーカスで候補ドロップダウン→一括入力。autocomplete属性+日本語ヒューリスティック(分割郵便番号・都道府県select・月/年select対応)。検証: `npx electron scripts/test-autofill-main.js` / `test-autofill-preload.js`
- **複数ウィンドウ**(Ctrl+N)、**シークレットウィンドウ**(Ctrl+Shift+N。紫の配色、履歴・パスワードを残さない、Cookieはメモリ内のみ)、**タブのドラッグ並べ替え**
- **画面分割**: タブを右クリック→「右に並べて表示」/「下に並べて表示」で2ペイン表示。ツールバーに方向切り替え・解除ボタンが出る(分割中のみ)
- **メディアプレイヤー**: 動画/音声の再生中に**そのタブを離れた時だけ**四隅常駐のフローティングミニプレイヤーが出現(再生中タブがアクティブ・分割相手の間は非表示。ドラッグで移動→離した位置に近い角へスナップ)。再生/一時停止・シーク・PinP(動画のみ)に対応。プレイヤー右クリック→「一時的に非表示」で隠せる(その再生が終わるか別タブの再生に変わると自動解除)。「サイドパネルに格納」でフローティングをやめ、サイドパネルの「再生中」セクションのみで操作する形に切り替え可能。検証: `npx electron scripts/test-media-player.js`
- **テーマ**(設定画面): アクセントカラー(プリセット+カラーピッカー)、新しいタブの背景(自動/固定/**画像アップロード**)、カスタムCSS(UIと内部ページに適用)。プロファイル単位+共有トグル対応
- **スタートページ**: Bonjourr風。時間帯で変わるグラデーション背景(夜明け5-8時/昼8-16時/夕暮れ16-19時/夜/画像)、大きな時計+日付+挨拶、すりガラスの検索欄。**独自ショートカット(ページ or ローカルフォルダ)を検索欄の下に配置**。アイコンは既定でリンク先のfavicon(取得できなければ頭文字)、プロファイルと同じ共通ピッカーで絵文字/画像に変更可。実体はブックマークの中の`start`フォルダで、直下の各サブフォルダが「ページ」(複数ページを切り替えドットで行き来できる)。星ボタン/バーはルート直下のみを見るが、**管理画面(Ctrl+Shift+O)ではブックマークと同じ1つのフォルダツリーとして管理・相互移動できる**
- **スタート画面ウィジェット**: **空いている場所を右クリック**すると出る追加メニューから天気(Open-Meteo。**場所の初期値は持たず**、イントロで設定した既定の場所→ウィジェット個別設定の順に使う。**検索APIは日本語の地名だと当たり外れがある**。「名古屋」「東京都」は当たるが「東京」「大阪」「札幌」「京都」は0件。ローマ字なら確実に当たるので、0件のときはローマ字を勧める文言を出す)/ノートパッド(自動保存)/カレンダー(月表示)/ニュース(RSS登録。プリセット=NHK/Yahoo!/テレビ朝日(ANN)/BBC News Japan/BBC News/CNN/WSJ/NYタイムズ。いずれも取得できることを確認済み。CNNはhttpsを提供しておらずhttpのみ)を追加(「+」アイコンは廃止)。⋮メニューで設定・更新・削除
  - **グリッドは座標ベース(Androidのホーム画面風)**: 各アイテムが固定の(列,行)座標を持ち、削除しても他のアイテムは動かない(自動上詰めなし)。ドラッグで並べ替え(掴んだ本体だけがポインタに追従、他は動かない)。ドロップ先が空きセルなら移動、同じ大きさのアイテムと重なれば入れ替え、それ以外は元の位置に戻る。ドラッグ・ウィジェットのリサイズ中はセルの区切り線+ドロップ先プレビュー(空き=緑/入れ替え=黄/範囲外=赤)をリアルタイム表示
  - **ウィジェットは右下のハンドルをドラッグしてサイズ変更可**(2〜4セル四方、他のアイテムと重ならない範囲で伸縮)
  - **アイコンサイズは設定画面「スタート画面」で最大サイズ(48〜160px、既定96px)のみ手動設定**。列数・行数はウィンドウの大きさから自動計算し、ウィンドウをリサイズしてもアイコン自体の大きさは変わらない(グリッド全体の目安の横幅=700pxとウィンドウ幅-48pxの小さいほうに収まる列数を計算。縦は収まる行数を計算し、収まらない場合は行数を減らす→時計位置の上方向オフセットを緩める、の順で安全弁が効き時計が画面外に出ない)
  - **複数ページはトラックパッドの横スワイプ/タッチスワイプでも切り替え可能**(スライド+フェードのアニメーション、ドットクリックと併用可)
  - **ニュースのフィード編集**(⋮→「フィードを編集」): 定番フィードのボタン列+URL入力+追加済み一覧。**入りきらない分は設定UIごとスクロールする**(子要素は `flex-shrink: 0`。これが無いと一覧だけが数pxに潰れ、「追加しても何も起きない」ように見える)。追加済みの定番ボタンは✓付きで押せなくなる。一覧はURLでなくフィード名、✕で削除。不正URL・重複はその場でメッセージを出す。0件の間は「表示する」を押せない
  - 検証: `npx electron scripts/test-newtab-widgets.js`(このハーネスは `show:false` のため `capturePage()` が古いフレームを返す。**見た目の確認はスクショでなく getBoundingClientRect() の実測で行う**)

### 初回起動 / アップデート後の案内

- **初回起動**: `roopie://welcome` を最初のタブに開く。6ステップ(ようこそ → できること → 見た目 → 検索とプライバシー → お住まいの場所 → 完了)で、アクセントカラー・タブバー位置・検索エンジン・広告ブロック・天気の場所をその場で決められる(選んだ瞬間に反映)。「スキップ」でいつでも抜けられる
- **アップデート後**: `roopie://whatsnew` を最初のタブに開き、`src/renderer/pages/release-notes.json` の変更点を表示する
- **出し分けはアプリのバージョンではなく release-notes.json の先頭 version(0.1 / 0.2 …)で行う**。masterへのpushごとにビルド番号(0.1.<run>)が上がるため、バージョン一致で判定すると1コミットごとにポップアップが出てしまう。**VERSION_BASE を上げるときに release-notes.json へ1件足す**と、全ユーザーが起動時に1度だけ変更点を見る
- 状態は `userData/app-state.json`(`introDone` / `seenNotes` / `installedVersion`)。開発中は `ROOPIE_ONBOARDING=welcome|whatsnew|off` で強制表示できる
- 設定画面の「Roopieについて」からいつでも再表示できる(変更履歴は `roopie://whatsnew?all` で全件)。バージョン表示・「更新を確認」・GitHubを開くも同じ場所
- 検証: `npx electron scripts/test-onboarding.js`

### リリース(自動)

masterへpushすると `.github/workflows/release.yml` がwindows-latestでビルドし、GitHub Releasesへ公開する。インストール済みのRoopieは起動時+15分ごとにReleasesを確認し、新しければ裏でダウンロードして再起動を促す。**手動で `npm run release` を叩く必要はない**(ローカル確認だけしたいときは `npm run dist`)。

- バージョン: `0.1.<Actionsの実行番号>` を**ビルド時にだけ**適用。package.jsonは0.1.0のまま=CIがmasterへpushし返さないので無限ループしない。メジャー/マイナーを上げるときはワークフローの `VERSION_BASE` を変える
- **このワークフローファイルはリネーム・移動しない**(実行番号が1に戻り、バージョンが逆行して自動更新が止まる)
- `publish.releaseType: "release"` は必須(既定の `draft` だとelectron-updaterから見えない)
- 公開状態のリリースはタグが先に必要なため、ビルド対象のコミットにタグを打ってから公開する。またelectron-builderは成果物を並列アップロードして同じタグのリリースを二重に作ることがあるため、**空のリリースを先に作ってからビルドする**
- 成果物は1回約100MBなので古いリリースは直近10件だけ残す(手動作成のv0.1.0もいずれ消える)。`**.md` だけの変更ではビルドしない
- 反映までの目安: push → ビルド約2分 → アプリが最大15分後に検知 → 再起動で適用
- インストーラーはNSISのアシスト形式(oneClick:false / ユーザー単位)。インストール先の変更・デスクトップショートカット・完了後の起動に対応。左側とヘッダーの画像は `npx electron scripts/gen-installer-art.js` で生成する(build/*.bmp。NSISは24bit BMPのみ対応のため自前で組み立てている)
- **アンインストール時に「ユーザーデータを残すか」を尋ねる**(`build/installer.nsh` の `customUnInstall`)。既定は「残す」で、いいえを選んだときだけ `%APPDATA%\Roopie` を削除する。**自動アップデート(--updated)やサイレント実行では尋ねず必ず残す**(これがないと更新のたびにデータが消える)

### 既知の制約・未対応

- **uBlock Originなどのブロッキング型拡張は動かない**(Electronの制約。内蔵広告ブロックで代替済み。詳細は下の Phase 2 検証記録)
- 画面分割は非アクティブペイン(相方タブ)側でツールバー(戻る/進む/ズーム/アドレスバー)を操作できない。ページ内でのクリック・スクロール・右クリックメニューは相方ペインでも正しく動く
- シークレットウィンドウでは拡張機能が動かない(Electronの制約: 非永続セッションは非対応)
- 起動ログの `blink.mojom.WidgetHost` エラーはChromiumの無害なノイズ(対応不要)

## 今後の計画

要件定義書のPhase 1〜6は一通り完了。残りは以下。

1. サイドパネルの残り機能(カレンダー/TODO。要件定義書 4.2)※リーディングリスト・ダウンロードは完了。AIチャットは廃止決定(2026-07-17、ユーザー指示)
2. デザインの継続改善(Preline UIのコンポーネントパターン適用を広げる)
3. 画面分割の発展: ドラッグ&ドロップでの分割(タブを別タブへドロップ)、ペイン間リサイズ、非アクティブペインからの戻る/進む/ズーム操作
4. メディアプレイヤーの発展: next/previous操作(MediaSession Action Handlerの都合上、汎用的な実装は不可。サイト個別対応が必要)、複数メディア同時再生時のUI改善

---

詳細な日付別の進捗記録は [log-archive.md](log-archive.md) に退避(通常は参照しない)。
以後、新しい詳細記録は log-archive.md に追記し、この log.md は「現在の状態」の要約のみ最新に保つ。
